<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/mainColorPercentLeft"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:text="@string/color_percent"
        android:textSize="@dimen/fontSize18"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/mainColorPercentRight"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:text="@string/color_percent"
        android:textSize="@dimen/fontSize18"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/mainColorPercent"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        app:layout_constraintBottom_toBottomOf="@id/mainColorPercentLeft"
        app:layout_constraintEnd_toStartOf="@id/mainColorPercentRight"
        app:layout_constraintLeft_toRightOf="@id/mainColorPercentLeft"
        app:layout_constraintRight_toLeftOf="@id/mainColorPercentRight"
        app:layout_constraintStart_toEndOf="@id/mainColorPercentLeft"
        app:layout_constraintTop_toTopOf="@id/mainColorPercentLeft" />

</androidx.constraintlayout.widget.ConstraintLayout>